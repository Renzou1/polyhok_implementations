require PolyHok

PolyHok.defmodule NORM do

defd square(x) do
  return x * x
end

defd add(x) do
  return x + x
end


x = Nx.tensor([1.0, 2.0, 3.0, 4.0])

prev = System.monotonic_time()


next = System.monotonic_time()


end
